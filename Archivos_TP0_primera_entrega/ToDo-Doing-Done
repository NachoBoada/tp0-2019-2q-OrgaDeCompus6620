#El formato es: 
#(tab) Quien-Lo que hizo o está haciendo. (salvo en ToDo que va solo lo que se tiene que hacer). (Dejar una linea entre ToDo, Doing y Done).

ToDo:
    -Automatizar validación de Pruebas. Ver que el output de una prueba sea el esperado. Podemos pasar el output a un archivo y 
    comparar caracter por caracter con otro que sea el validador de la prueba. Onda: cat file1.txt | tp0 > file1out.txt, y el out.txt compararlo con "ValidadorDefile1.txt" (no sé cómo hacer esto de comparar).
    
    
Doing:

Done:
    Nacho-Creacion del Repo

    Axel-Make up
    Axel-Manejo de comandos
    Axel-Probar que el programa corre correctamente en NetBSD/pmax(usando el simulador GXemul [1]) y Linux. (Portabilidad).

    Mauro-readMatrixDimention
    Mauro - matrix_multiply

    Nacho-create_matrix
    Nacho-destroy_matrix
    Nacho-fillUpMatrices

    Mauro - matrix_multiply

    Axel - error (Ante un error, el progama deber ́a informar la situaci ́on inmediatamente (porstderr) ydetener su ejecución).
    Axel - print_matrix (que haga output por stdout)

    Axel - Agregar funcionalidad para leer más de una línea por stdin (leer hsata la señal de EOF).

    Axel - Informe (creo que se puede separar en subpartes como Informe:Introduccion, Informe:Diseño, etc..).
    Nacho-Generar Pruebas (leer ParaHacerPruebas.txt).
    
    -Informe (creo que se puede separar en subpartes como Informe:Introduccion, Informe:Diseño, etc..).
