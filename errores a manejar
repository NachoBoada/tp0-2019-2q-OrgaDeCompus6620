errores posibles:

que la dimension no sea un int

que no ingrese la dimension

que no respete la cantidad de elementos prometidos por la dimension#

que cada vez que reservo en memoria falle#

*) cada vez que hay un error liberar memoria
